[project]
name = "my_default_project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "afrossard", email = "aurelien.frossard@gmail.com" }
]

requires-python = "==3.14.*"
dependencies = [
]

[project.scripts]
main = "my_default_project.main:main"

[build-system]
requires = ["uv_build>=0.9.12,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.1,<10",
    "black>=26.5.1,<27",
]

